# DiffBudget Tasks

- Scaffold TypeScript CLI package.
- Implement budget config discovery and init command.
- Parse git diffs and compute local risk scores.
- Emit Markdown and JSON reports.
- Add fixtures, tests, and smoke checks.
- Document safety model and practical examples.
